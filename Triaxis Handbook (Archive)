AMD64

Welcome To Triaxis GNU/Linux Handbook AMD64
This Guide Will Help You To Install Triaxis Correctly

1 enter https://ubuntu.com/download/server then download ubuntu server lts iso then Before You Install Flash Ubuntu Server Iso With Rufus Etcher Or Ventoy
Make Sure That There Are No important Files Inside the USB Drive Then Backup Your Data On Your System Because It Will Erase Your Partition Drive After Installing Triaxis
For Rufus There are both ISO mode (the default, file-copy method) and DD mode (a block-level copy method) options
and I Would Recommend To Use dd mode Because On ISO mode it Can fail Because ISO mode partitions and formats the USB like a standard drive

2 Boot from the Ubuntu Server ISO
  Press Ctrl+Alt+F2 to switch to a shell.
  Theres Password on su -
  I Would recommend to change the password to your password using sudo passwd root for example triaxis or become root using sudo -i
3 Partition the Disk Using cfdisk fdisk parted etc
  cfdisk /dev/sda
  Create partitions:

4 EFI (if UEFI): 512MB, type EFI System.
  Root (/): rest of disk, type Linux filesystem.
  Swap: optional, type Linux swap.
  Write changes and quit.

5 Format Partitions
  mkfs.ext4 /dev/sda2     # root
  mkswap /dev/sda3        # swap
  mkfs.fat -F32 /dev/sda1 # EFI (if UEFI)

6 Mount Target System
  mount /dev/sda2 /mnt
  mkdir /mnt/boot
  mkdir /mnt/boot/efi
  mount /dev/sda1 /mnt/boot/efi
  swapon /dev/sda3

7 Bootstrap Base System
  Before You Do It Run apt install debootstrap
  debootstrap --arch amd64 yourubuntucodenameexamplenoble /mnt http://archive.ubuntu.com/ubuntu

8 Chroot Into New System

  mount --bind /dev /mnt/dev
  mount --bind /proc /mnt/proc
  mount --bind /sys /mnt/sys
  chroot /mnt

9 Configure System
  Inside chroot:
  echo "myhostname" > /etc/hostname
  nano /etc/hosts   # add 127.0.0.1 myhostname

10 Set up networking:
   apt install net-tools ifupdown

11 Install Kernel & Essentials
   apt install linux-image-generic grub-pc sudo vim

12 Install Bootloader
   For BIOS:
   grub-install /dev/sda
   update-grub

13 For UEFI:
   apt install grub-efi
   grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=ubuntu
   update-grub

14 Create User
   adduser yourname
   usermod -aG sudo yourname

15 Exit & Reboot
   exit
   umount -R /mnt
   swapoff -a
   reboot

16 Update Using sudo apt update
   
Remove Snapd

1 You May Stop The Services Of Snap
  sudo systemctl stop snapd
  sudo systemctl disable snapd

2 Purge Snapd (Recommended For Older Hardware Or Any Slow Hardware)
  sudo apt purge snapd

3 Clean Up Leftover Directories
  rm -rf ~/snap
  sudo rm -rf /var/snap /var/lib/snapd /var/cache/snapd

4 Reboot
  sudo reboot

5 System Updates: Ubuntu may reinstall Snapd during major upgrades. To prevent this, you can block Snapd from being reinstalled:
  sudo apt-mark hold snapd

17 Install Kde Plasma Desktop sudo apt install plasma-workspace-wayland konsole for base and servers dont install desktop environment if you Want Other Desktop Environment Go To https://github.com/Haikal02-alt/Triaxis-GNU-Linux/blob/main/Other%20Desktop%20Environment
   After You Install KDE Plasma Run these commands
   sudo apt install sddm
   sudo systemctl enable sddm
   sudo systemctl start sddm
Change Grub If You Need It Run sudo nano /etc/default/grub then
GRUB_TIMEOUT=0
Change them to:
GRUB_TIMEOUT_STYLE=menu
GRUB_TIMEOUT=5
update grub using sudo update-grub
18 Change GRUB Distributor Name Ubuntu To Triaxis Change it to: GRUB_DISTRIBUTOR="Triaxis"
Then Update grub using sudo update-grub
19 enable submenu sudo nano /etc/grub.d/10_linux then change GRUB_DISABLE_SUBMENU="true" to GRUB_DISABLE_SUBMENU="false" then sudo update-grub
20 Add A logo use figlet to install it use sudo apt install figlet then figlet TRI then press prt sc then click rectangular region then screenshot the TRI on figlet then name it to triaxis-logo.png then move the image to /usr/share/pixmaps/ sudo cp ~/your TRI Logo/triaxis-logo.png /usr/share/pixmaps/
Or Install firefox or konqueror Then You Can Download Triaxis Logo On Readme then theres a triaxis logo then Save Image As /home/yourname/Pictures then name it to Triaxis-logo.png then Open a terminal.
Use the mv command with sudo:
sudo mv /home/yourname/Picture/Triaxis-logo.png /usr/share/pixmaps/
then sudo cp /usr/share/pixmaps/Triaxis-logo.png /usr/share/pixmaps/ubuntu-logo.svg
then sudo cp /usr/share/pixmaps/Triaxis-logo.png /usr/share/pixmaps/ubuntu-logo-text.png
then sudo cp /usr/share/pixmaps/Triaxis-logo.png /usr/share/pixmaps/ubuntu-logo-text-dark.png
the open sudo nano /etc/os-release then you find logo= then Change to LOGO=/usr/share/pixmaps/Triaxis-logo.png
if you cant see the change logo heres a guided version then follow the capital word
1 GO TO sudo nano /etc/os-release
2 THEN CHANGE LOGO=ubuntu-logo TO LOGO=/usr/share/pixmaps/Triaxis-logo.png
3 THEN CTRL X Y ENTER
(For New Users Only) Remove Plymouth Run sudo nano /etc/default/grub then GRUB_CMDLINE_LINUX_DEFAULT="" then sudo update-grub then sudo apt purge plymouth; sudo apt autoremove
Then Reboot

21 Replace The SDDM Use sudo apt install sddm-theme-lubuntu Locate Available SDDM Themes If you’re getting “Permission denied” when trying to change SDDM to use the LXQt theme, it usually means you don’t have the right privileges or the file isn’t writable by your user. Here’s how to fix it:
Run sudo nano /etc/sddm.conf If the file doesn’t exist, create it with root Generate a default config: sudo sddm --example-config > /etc/sddm.conf then edit it sudo nano /etc/sddm.conf Set LXQt theme in the config
Inside /etc/sddm.conf, add: [Theme]
Current=lubuntu
Restart SDDM Using sudo systemctl restart sddm
Themes are usually stored in: /usr/share/sddm/themes/
Edit the SDDM config file: with sudo nano /etc/sddm.conf If the file doesn’t exist, create it and add: [Theme]
Current=lubuntu
Or use the command sudo sddm --example-config > /etc/sddm.conf
Restart SDDM sudo systemctl restart sddm
You can Also go to settings then startup and shutdown then login screen sddm then click lubuntu then apply
Install Firefox

1 git clone https://github.com/mozilla/gecko-dev.git
cd gecko-dev

2 sudo apt update
  sudo apt install build-essential autoconf2.13 python3 python3-pip \
       cargo clang libgtk-3-dev libdbus-glib-1-dev libxt-dev

3 ./mach bootstrap

4 ./mach build

5 ./mach run

if you cant install firefox you can install other browsers like konqueror sudo apt install konqueror
22 Enable The Login And Logout Sound Open System Settings Then Personalization Then Notification Then Application-specific settings then configure then system services plasma workspace configure events then click Play a sound on login and logout sounds
23 Set ascii_distro="none" on nano ~/.config/neofetch/config.conf Because We Dont Need Neofetch Logo
24 Change the Link Open the os-release file sudo nano /etc/os-release
Find the line that looks like this: HOME_URL="https://www.ubuntu.com/"
Replace it to HOME_URL="https://github.com/Haikal02-alt/Triaxis-GNU-Linux"
Then Pretty Name And Name To Triaxis then Change Version_id and version to
(Optional) You can also change other fields:
NAME="Triaxis GNU/Linux"
PRETTY_NAME="Triaxis GNU/Linux"
ID=triaxis
VERSION=""
VERSION_ID=""
VERSION_CODENAME=noble to VERSION_CODENAME=
Save and exit (CTRL+O, CTRL+X in nano).
Restart Plasma session (log out and back in, or reboot).
Now, when you open About System, the link will point to your GitHub page.

25 (For New User Only) Add Flatpak Backends On KDE Plasma Run sudo apt install plasma-discover-backend-flatpak
If You Havent Install Flatpak sudo apt install flatpak
If You Are using GNOME
sudo apt install gnome-software-plugin-flatpak
Add the Flathub repository
flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo
Reboot
Remove VERSION_ID=""
VERSION=""
VERSION_CODENAME=""
And Added BUILD_ID=rolling
Example
PRETTY_NAME="Triaxis GNU/Linux"
NAME="Triaxis GNU/Linux"
ID=triaxis
ID_LIKE=debian
BUILD_ID=rolling
How To Do It
1 Open sudo nano /etc/os-release
2 Remove
    VERSION_ID=""
    VERSION=""
    VERSION_CODENAME=""
3 Add BUILD_ID=rolling After The ID_LIKE=debian
    Example
    PRETTY_NAME="Triaxis GNU/Linux"
    NAME="Triaxis GNU/Linux"
    ID=triaxis
    ID_LIKE=debian
    BUILD_ID=rolling
    Remove UBUNTU_CODENAME Because Its A Rolling Release Distro
4 Ctrl X Y Enter
5 sudo apt update && sudo apt upgrade -y

26 Change The Base Name UBUNTU TO Triaxis
sudo nano /etc/issue then change The Name Ubuntu (Version Name) LTS To Triaxis GNU/Linux
sudo nano /etc/issue.net then rename Ubuntu (Version Name) LTS To Triaxis GNU/Linux
Then type sudo reboot Or type logout

27 (For New User Only) Setup The Artwork Theme
1 Download The Triaxis Official Artwork Theme Slasher Here
2 Then Click Save Image As
3 Save It To Download Then Name It To Slasher.png
4 Then Create Folder Name Slasher
5 Then Move Slasher.png To Folder Name Slasher
6 Open Terminal Then sudo mv /home/yourusername/Slasher /usr/share/wallpapers/
7 On KDE Plasma 
  Right‑click on the desktop → Configure Desktop and Wallpaper. Under Wallpaper, choose Image. Click Open and select your Slasher wallpaper file.
  Adjust scaling (Scaled, Centered, or Fill) depending on your preference.
  Apply → the Slasher Wallpaper
  
  On GNOME
  Open Settings → Appearance.
  Under Background, click the image selector.
  Choose your Slasher wallpaper file.
  GNOME lets you set different images for light mode and dark mode if desired.
  Apply → the Slasher becomes your GNOME background.

  On Sway (Wayland tiling WM)
  Edit your Sway config file (usually ~/.config/sway/config).
  Add a line like:
  output * bg /usr/share/wallpapers/Slasher/Slasher.png fill
  fill ensures it scales properly.
  Save the config and reload Sway (Super+Shift+C).

  On XFCE
  Right‑click on the desktop → Desktop Settings.
  Under Background, click Folder or Image.
  Select your Slasher wallpaper file.
  Choose style (Scaled, Zoom, Centered, etc.).
  Apply

8 You Can Select Slasher On SDDM
  Open System Settings.
  Go to Startup and Shutdown → Login Screen (SDDM).
  In the Theme section, pick the theme you want (e.g., Lubuntu, or any custom one).
  Click Background (some themes show this option directly).
  Browse and select your Slasher wallpaper image.
  Apply changes.
  Restart or log out

28 (For New User Only)
   Add The Wiki On /etc/os-release add DOCUMENTATION_URL=https://github.com/Haikal02-alt/Triaxis-GNU-Linux/wiki
   Run sudo nano /etc/os-release
   then add DOCUMENTATION_URL=https://github.com/Haikal02-alt/Triaxis-GNU-Linux/wiki after the HOME_URL
   Example
   BUILD_ID=rolling
   HOME_URL="https://github.com/Haikal02-alt/Triaxis-GNU-Linux"
   DOCUMENTATION_URL=https://github.com/Haikal02-alt/Triaxis-GNU-Linux/wiki

For Base Install
Run sudo nano /etc/os-release
Then Change Ubuntu To Triaxis GNU/Linux
Example

PRETTY_NAME="Triaxis GNU/Linux"
NAME="Triaxis GNU/Linux"
ID=triaxis
ID_LIKE=debian
BUILD_ID=rolling
HOME_URL="https://github.com/Haikal02-alt/Triaxis-GNU-Linux"
DOCUMENTATION_URL=https://github.com/Haikal02-alt/Triaxis-GNU-Linux/wiki

Keep The Support URL Bug Report URL And Privacy Policy URL Ubuntu

Then sudo nano /etc/issue
Then Change Ubuntu (Version Name LTS) \n \l To Triaxis GNU/Linux \n \l
CTRL X Y ENTER

Then sudo nano /etc/issue.net
Then Change Ubuntu (Version Name LTS) To Triaxis GNU/Linux
CTRL X Y ENTER

Change Grub If You Need It Run sudo nano /etc/default/grub then
GRUB_TIMEOUT=0
Change them to:
GRUB_TIMEOUT_STYLE=menu
GRUB_TIMEOUT=5
update grub using sudo update-grub
Change GRUB Distributor Name Ubuntu To Triaxis Change it to: GRUB_DISTRIBUTOR="Triaxis"
Then Update grub using sudo update-grub
enable submenu sudo nano /etc/grub.d/10_linux then change GRUB_DISABLE_SUBMENU="true" to GRUB_DISABLE_SUBMENU="false" then sudo update-grub

29 Run sudo update-initramfs -u

30 Have Fun And Start Using Triaxis And You can Say I USE Triaxis BTW

ARM

Welcome To Triaxis GNU/Linux Handbook ARM
This Guide Will Help You To Install Triaxis Correctly

1 enter https://ubuntu.com/download/server/arm then download ubuntu server lts iso then Before You Install Flash Ubuntu Server Iso With Rufus Etcher Or Ventoy
Make Sure That There Are No important Files Inside the USB Drive Then Backup Your Data On Your System Because It Will Erase Your Partition Drive After Installing Triaxis
For Rufus There are both ISO mode (the default, file-copy method) and DD mode (a block-level copy method) options
and I Would Recommend To Use dd mode Because On ISO mode it Can fail Because ISO mode partitions and formats the USB like a standard drive

2 Boot from the Ubuntu Server ISO
  Theres Password on su -
  I Would recommend to change the password to your password using sudo passwd root for example triaxis or become root using sudo -i
  Press Ctrl+Alt+F2 to switch to a shell.
3 Partition the Disk Using cfdisk fdisk parted etc
  cfdisk /dev/sda
  Create partitions:

4 EFI (if UEFI): 512MB, type EFI System.
  Root (/): rest of disk, type Linux filesystem.
  Swap: optional, type Linux swap.
  Write changes and quit.

5 Format Partitions
  mkfs.ext4 /dev/sda2     # root
  mkswap /dev/sda3        # swap
  mkfs.fat -F32 /dev/sda1 # EFI (if UEFI)

6 Mount Target System
  mount /dev/sda2 /mnt
  mkdir /mnt/boot
  mkdir /mnt/boot/efi
  mount /dev/sda1 /mnt/boot/efi
  swapon /dev/sda3

7 Bootstrap Base System
  Before You Do It Run apt install debootstrap
  debootstrap --arch armhf ubuntucodenameexamplenoble /mnt http://archive.ubuntu.com/ubuntu
  For ARM64
  debootstrap --arch arm64 ubuntucodenameexamplenoble /mnt http://archive.ubuntu.com/ubuntu

8 Chroot Into New System

  mount --bind /dev /mnt/dev
  mount --bind /proc /mnt/proc
  mount --bind /sys /mnt/sys
  chroot /mnt

9 Configure System
  Inside chroot:
  echo "myhostname" > /etc/hostname
  nano /etc/hosts   # add 127.0.0.1 myhostname

10 Set up networking:
   apt install net-tools ifupdown

11 Install Kernel & Essentials
   apt install linux-image-generic grub-pc sudo vim

12 Install Bootloader
   For BIOS:
   grub-install /dev/sda
   update-grub

   For UEFI:
   apt install grub-efi
   grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=ubuntu
   update-grub

13 Create User
   adduser yourname
   usermod -aG sudo yourname

14 Exit & Reboot
   exit
   umount -R /mnt
   swapoff -a
   reboot

15 Run sudo nano /etc/os-release
Then Change Ubuntu To Triaxis GNU/Linux
Example

PRETTY_NAME="Triaxis GNU/Linux ARM"
NAME="Triaxis GNU/Linux ARM"
ID=triaxis
ID_LIKE=debian
BUILD_ID=rolling
HOME_URL="https://github.com/Haikal02-alt/Triaxis-GNU-Linux"
DOCUMENTATION_URL=https://github.com/Haikal02-alt/Triaxis-GNU-Linux/wiki

Keep The Support URL Bug Report URL And Privacy Policy URL Ubuntu

Then sudo nano /etc/issue
Then Change Ubuntu (Version Name LTS) \n \l To Triaxis GNU/Linux ARM \n \l
CTRL X Y ENTER

Then sudo nano /etc/issue.net
Then Change Ubuntu (Version Name LTS) To Triaxis GNU/Linux ARM
CTRL X Y ENTER

16 Change Grub If You Need It Run sudo nano /etc/default/grub then
GRUB_TIMEOUT=0
Change them to:
GRUB_TIMEOUT_STYLE=menu
GRUB_TIMEOUT=5
update grub using sudo update-grub
Change GRUB Distributor Name Ubuntu To Triaxis Change it to: GRUB_DISTRIBUTOR="Triaxis ARM"
Then Update grub using sudo update-grub
enable submenu sudo nano /etc/grub.d/10_linux then change GRUB_DISABLE_SUBMENU="true" to GRUB_DISABLE_SUBMENU="false" then sudo update-grub

17 You May Stop The Services Of Snap
   sudo systemctl stop snapd
   sudo systemctl disable snapd

18 Purge Snapd (Recommended For Older Hardware Or Any Slow Hardware)
   sudo apt purge snapd

19 Clean Up Leftover Directories
   rm -rf ~/snap
   sudo rm -rf /var/snap /var/lib/snapd /var/cache/snapd

20 Reboot
   sudo reboot

21 System Updates: Ubuntu may reinstall Snapd during major upgrades. To prevent this, you can block Snapd from being reinstalled:
   sudo apt-mark hold snapd

22 Run sudo update-initramfs -u

23 Have Fun And Start Using Triaxis ARM

IBM Power

Welcome To Triaxis GNU/Linux Handbook IBM POWER
This Guide Will Help You To Install Triaxis Correctly

1 enter https://ubuntu.com/download/server/power then download ubuntu server lts iso then Before You Install Flash Ubuntu Server Iso With Rufus Etcher Or Ventoy
Make Sure That There Are No important Files Inside the USB Drive Then Backup Your Data On Your System Because It Will Erase Your Partition Drive After Installing Triaxis
For Rufus There are both ISO mode (the default, file-copy method) and DD mode (a block-level copy method) options
and I Would Recommend To Use dd mode Because On ISO mode it Can fail Because ISO mode partitions and formats the USB like a standard drive

2 Boot from the Ubuntu Server ISO
  Theres Password on su -
  I Would recommend to change the password to your password using sudo passwd root for example triaxis or become root using sudo -i
  Press Ctrl+Alt+F2 to switch to a shell.
3 Partition the Disk Using cfdisk fdisk parted etc
  cfdisk /dev/sda
  Create partitions:

4 EFI (if UEFI): 512MB, type EFI System.
  Root (/): rest of disk, type Linux filesystem.
  Swap: optional, type Linux swap.
  Write changes and quit.

5 Format Partitions
  mkfs.ext4 /dev/sda2     # root
  mkswap /dev/sda3        # swap
  mkfs.fat -F32 /dev/sda1 # EFI (if UEFI)

6 Mount Target System
  mount /dev/sda2 /mnt
  mkdir /mnt/boot
  mkdir /mnt/boot/efi
  mount /dev/sda1 /mnt/boot/efi
  swapon /dev/sda3

7 Bootstrap Base System
  Before You Do It Run apt install debootstrap
  debootstrap --arch ppc64el ubuntucodenameexamplenoble /mnt http://archive.ubuntu.com/ubuntu

8 Chroot Into New System

  mount --bind /dev /mnt/dev
  mount --bind /proc /mnt/proc
  mount --bind /sys /mnt/sys
  chroot /mnt

9 Configure System
  Inside chroot:
  echo "myhostname" > /etc/hostname
  nano /etc/hosts   # add 127.0.0.1 myhostname

10 Set up networking:
   apt install net-tools ifupdown

11 Install Kernel & Essentials
   apt install linux-image-generic grub-pc sudo vim

12 Install Bootloader
   For BIOS:
   grub-install /dev/sda
   update-grub

   For UEFI:
   apt install grub-efi
   grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=ubuntu
   update-grub

13 Create User
   adduser yourname
   usermod -aG sudo yourname

14 Exit & Reboot
   exit
   umount -R /mnt
   swapoff -a
   reboot

15 Run sudo nano /etc/os-release
Then Change Ubuntu To Triaxis GNU/Linux IBM POWER
Example

PRETTY_NAME="Triaxis GNU/Linux IBM POWER"
NAME="Triaxis GNU/Linux IBM POWER"
ID=triaxis
ID_LIKE=debian
BUILD_ID=rolling
HOME_URL="https://github.com/Haikal02-alt/Triaxis-GNU-Linux"
DOCUMENTATION_URL=https://github.com/Haikal02-alt/Triaxis-GNU-Linux/wiki

Keep The Support URL Bug Report URL And Privacy Policy URL Ubuntu

Then sudo nano /etc/issue
Then Change Ubuntu (Version Name LTS) \n \l To Triaxis GNU/Linux IBM POWER \n \l
CTRL X Y ENTER

Then sudo nano /etc/issue.net
Then Change Ubuntu (Version Name LTS) To Triaxis GNU/Linux IBM POWER
CTRL X Y ENTER

16 Change Grub If You Need It Run sudo nano /etc/default/grub then
GRUB_TIMEOUT=0
Change them to:
GRUB_TIMEOUT_STYLE=menu
GRUB_TIMEOUT=5
update grub using sudo update-grub
Change GRUB Distributor Name Ubuntu To Triaxis Change it to: GRUB_DISTRIBUTOR="Triaxis IBM POWER"
Then Update grub using sudo update-grub
enable submenu sudo nano /etc/grub.d/10_linux then change GRUB_DISABLE_SUBMENU="true" to GRUB_DISABLE_SUBMENU="false" then sudo update-grub

17 You May Stop The Services Of Snap
   sudo systemctl stop snapd
   sudo systemctl disable snapd

18 Purge Snapd (Recommended For Older Hardware Or Any Slow Hardware)
   sudo apt purge snapd

19 Clean Up Leftover Directories
   rm -rf ~/snap
   sudo rm -rf /var/snap /var/lib/snapd /var/cache/snapd

20 Reboot
   sudo reboot

21 System Updates: Ubuntu may reinstall Snapd during major upgrades. To prevent this, you can block Snapd from being reinstalled:
  sudo apt-mark hold snapd

22 Run sudo update-initramfs -u

23 Have Fun And Start Using Triaxis IBM Power

IBM Z and LinuxONE

1 Enter https://ubuntu.com/download/server/s390x Which Is LTS
2 Then download ubuntu server lts iso then Before You Install Flash Ubuntu Server Iso With Rufus Etcher Or Ventoy
Make Sure That There Are No important Files Inside the USB Drive Then Backup Your Data On Your System Because It Will Erase Your Partition Drive After Installing Triaxis
For Rufus There are both ISO mode (the default, file-copy method) and DD mode (a block-level copy method) options
and I Would Recommend To Use dd mode Because On ISO mode it Can fail Because ISO mode partitions and formats the USB like a standard drive
3 Boot from the Ubuntu Server ISO
  Theres Password on su -
  I Would recommend to change the password to your password using sudo passwd root for example triaxis or become root using sudo -i
  Press Ctrl+Alt+F2 to switch to a shell.
4 Partition the Disk Using cfdisk fdisk parted etc
  cfdisk /dev/sda
  Create partitions:

5 EFI (if UEFI): 512MB, type EFI System.
  Root (/): rest of disk, type Linux filesystem.
  Swap: optional, type Linux swap.
  Write changes and quit.

6 Format Partitions
  mkfs.ext4 /dev/sda2     # root
  mkswap /dev/sda3        # swap
  mkfs.fat -F32 /dev/sda1 # EFI (if UEFI)

7 Mount Target System
  mount /dev/sda2 /mnt
  mkdir /mnt/boot
  mkdir /mnt/boot/efi
  mount /dev/sda1 /mnt/boot/efi
  swapon /dev/sda3

8 Bootstrap Base System
  debootstrap --arch s390x yourubuntucodenameexamplenoble /mnt http://archive.ubuntu.com/ubuntu

9 Chroot Into New System

  mount --bind /dev /mnt/dev
  mount --bind /proc /mnt/proc
  mount --bind /sys /mnt/sys
  chroot /mnt

10 Configure System
   Inside chroot:
   echo "myhostname" > /etc/hostname
   nano /etc/hosts   # add 127.0.0.1 myhostname

11 Set up networking:
   apt install net-tools ifupdown

12 Install Kernel & Essentials
   apt install linux-image-generic grub-pc sudo vim

13 Install Bootloader
   For BIOS:
   grub-install /dev/sda
   update-grub

   For UEFI:
   apt install grub-efi
   grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=ubuntu
   update-grub

14 Create User
   adduser yourname
   usermod -aG sudo yourname

15 Exit & Reboot
   exit
   umount -R /mnt
   swapoff -a
   reboot

16 Run sudo nano /etc/os-release
Then Change Ubuntu To Triaxis GNU/Linux
Example

PRETTY_NAME="Triaxis GNU/Linux IBM Z and LinuxONE"
NAME="Triaxis GNU/Linux IBM Z and LinuxONE"
ID=triaxisibmzandlinuxone
ID_LIKE=debian
BUILD_ID=rolling
HOME_URL="https://github.com/Haikal02-alt/Triaxis-GNU-Linux"
DOCUMENTATION_URL=https://github.com/Haikal02-alt/Triaxis-GNU-Linux/wiki

Keep The Support URL Bug Report URL And Privacy Policy URL Ubuntu

Then sudo nano /etc/issue
Then Change Ubuntu (Version Name LTS) \n \l To Triaxis GNU/Linux IBM Z and LinuxONE \n \l
CTRL X Y ENTER

Then sudo nano /etc/issue.net
Then Change Ubuntu (Version Name LTS) To Triaxis GNU/Linux IBM Z and LinuxONE
CTRL X Y ENTER

17 Change Grub If You Need It Run sudo nano /etc/default/grub then
GRUB_TIMEOUT=0
Change them to:
GRUB_TIMEOUT_STYLE=menu
GRUB_TIMEOUT=5
update grub using sudo update-grub
Change GRUB Distributor Name Ubuntu To Triaxis Change it to: GRUB_DISTRIBUTOR="Triaxis IBM Z and LinuxONE"
Then Update grub using sudo update-grub
enable submenu sudo nano /etc/grub.d/10_linux then change GRUB_DISABLE_SUBMENU="true" to GRUB_DISABLE_SUBMENU="false" then sudo update-grub

18 You May Stop The Services Of Snap
   sudo systemctl stop snapd
   sudo systemctl disable snapd

19 Purge Snapd (Recommended For Older Hardware Or Any Slow Hardware)
   sudo apt purge snapd

20 Clean Up Leftover Directories
   rm -rf ~/snap
   sudo rm -rf /var/snap /var/lib/snapd /var/cache/snapd

21 Reboot
   sudo reboot

22 System Updates: Ubuntu may reinstall Snapd during major upgrades. To prevent this, you can block Snapd from being reinstalled:
   sudo apt-mark hold snapd

23 Run sudo update-initramfs -u

24 Have Fun And Start Using Triaxis IBM Z and LinuxONE

RISC-V

Welcome To Triaxis GNU/Linux Handbook RISC-V
This Guide Will Help You To Install Triaxis Correctly

1 enter https://ubuntu.com/download/risc-v/canonical-built#allwinner-nezha then Choose a board then download ubuntu server lts iso then Before You Install Flash Ubuntu Server Iso With Rufus Etcher Or Ventoy
Make Sure That There Are No important Files Inside the USB Drive Then Backup Your Data On Your System Because It Will Erase Your Partition Drive After Installing Triaxis
For Rufus There are both ISO mode (the default, file-copy method) and DD mode (a block-level copy method) options
and I Would Recommend To Use dd mode Because On ISO mode it Can fail Because ISO mode partitions and formats the USB like a standard drive

2 Boot from the Ubuntu Server ISO
  Theres Password on su -
  I Would recommend to change the password to your password using sudo passwd root for example triaxis or become root using sudo -i
  Press Ctrl+Alt+F2 to switch to a shell.
3 Partition the Disk Using cfdisk fdisk parted etc
  cfdisk /dev/sda
  Create partitions:

4 EFI (if UEFI): 512MB, type EFI System.
  Root (/): rest of disk, type Linux filesystem.
  Swap: optional, type Linux swap.
  Write changes and quit.

5 Format Partitions
  mkfs.ext4 /dev/sda2     # root
  mkswap /dev/sda3        # swap
  mkfs.fat -F32 /dev/sda1 # EFI (if UEFI)

6 Mount Target System
  mount /dev/sda2 /mnt
  mkdir /mnt/boot
  mkdir /mnt/boot/efi
  mount /dev/sda1 /mnt/boot/efi
  swapon /dev/sda3

7 Bootstrap Base System
  debootstrap --arch riscv64 focal /mnt http://archive.ubuntu.com/ubuntu

8 Chroot Into New System

  mount --bind /dev /mnt/dev
  mount --bind /proc /mnt/proc
  mount --bind /sys /mnt/sys
  chroot /mnt

9 Configure System
  Inside chroot:
  echo "myhostname" > /etc/hostname
  nano /etc/hosts   # add 127.0.0.1 myhostname

10 Set up networking:
   apt install net-tools ifupdown

11 Install Kernel & Essentials
   apt install linux-image-generic grub-pc sudo vim

12 Install Bootloader
   For BIOS:
   grub-install /dev/sda
   update-grub

   For UEFI:
   apt install grub-efi
   grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=ubuntu
   update-grub

13 Create User
   adduser yourname
   usermod -aG sudo yourname

14 Exit & Reboot
   exit
   umount -R /mnt
   swapoff -a
   reboot

15 Run sudo nano /etc/os-release
Then Change Ubuntu To Triaxis GNU/Linux RISC-V
Example

PRETTY_NAME="Triaxis GNU/Linux RISC-V"
NAME="Triaxis GNU/Linux RISC-V"
ID=triaxis
ID_LIKE=debian
BUILD_ID=rolling
HOME_URL="https://github.com/Haikal02-alt/Triaxis-GNU-Linux"
DOCUMENTATION_URL=https://github.com/Haikal02-alt/Triaxis-GNU-Linux/wiki

Keep The Support URL Bug Report URL And Privacy Policy URL Ubuntu

Then sudo nano /etc/issue
Then Change Ubuntu (Version Name LTS) \n \l To Triaxis GNU/Linux RISC-V \n \l
CTRL X Y ENTER

Then sudo nano /etc/issue.net
Then Change Ubuntu (Version Name LTS) To Triaxis GNU/Linux RISC-V
CTRL X Y ENTER

16 Change Grub If You Need It Run sudo nano /etc/default/grub then
GRUB_TIMEOUT=0
Change them to:
GRUB_TIMEOUT_STYLE=menu
GRUB_TIMEOUT=5
update grub using sudo update-grub
Change GRUB Distributor Name Ubuntu To Triaxis Change it to: GRUB_DISTRIBUTOR="Triaxis RISC-V"
Then Update grub using sudo update-grub
enable submenu sudo nano /etc/grub.d/10_linux then change GRUB_DISABLE_SUBMENU="true" to GRUB_DISABLE_SUBMENU="false" then sudo update-grub

17 You May Stop The Services Of Snap
   sudo systemctl stop snapd
   sudo systemctl disable snapd

18 Purge Snapd (Recommended For Older Hardware Or Any Slow Hardware)
   sudo apt purge snapd

19 Clean Up Leftover Directories
   rm -rf ~/snap
   sudo rm -rf /var/snap /var/lib/snapd /var/cache/snapd

20 Reboot
   sudo reboot

21 System Updates: Ubuntu may reinstall Snapd during major upgrades. To prevent this, you can block Snapd from being reinstalled:
   sudo apt-mark hold snapd

22 Run sudo update-initramfs -u

23 Have Fun And Start Using Triaxis RISC-V

X86

Welcome To Triaxis GNU/Linux Handbook x86
This Guide Will Help You To Install Triaxis Correctly

1 Download Ubuntu Server 16.04 Here https://releases.ubuntu.com/16.04/
2 Then Select Server install image 32-bit PC (i386) server install image
3 When You On Live Iso Press F6 The Select Expert Mode Then Setup Ubuntu Server
4 After You Install

Run sudo apt update && sudo apt upgrade -y

Run sudo nano /etc/os-release
Then Change Ubuntu To Triaxis GNU/Linux
Example

NAME="Triaxis GNU/Linux X86"
ID=triaxis
ID_LIKE=debian
BUILD_ID=rolling
PRETTY_NAME="Triaxis GNU/Linux X86"
HOME_URL="https://github.com/Haikal02-alt/Triaxis-GNU-Linux"
SUPPORT_URL=
BUG_REPORT_URL=

Keep The Support URL Bug Report URL And Privacy Policy URL Ubuntu

Then sudo nano /etc/issue
Then Change Ubuntu 16.04 LTS \n \l To Triaxis GNU/Linux X86 \n \l
CTRL X Y ENTER

Then sudo nano /etc/issue.net
Then Change Ubuntu 16.04 LTS To Triaxis GNU/Linux X86
CTRL X Y ENTER

Change Grub If You Need It Run sudo nano /etc/default/grub then
GRUB_TIMEOUT=0
Change them to:
GRUB_TIMEOUT=5
update grub using sudo update-grub
Change GRUB Distributor Name Ubuntu To Triaxis Change it to: GRUB_DISTRIBUTOR="Triaxis X86"
Then Update grub using sudo update-grub

Reboot using sudo reboot

5 You May Stop The Services Of Snap
  sudo systemctl stop snapd
  sudo systemctl disable snapd

6 Purge Snapd (Recommended For Older Hardware Or Any Slow Hardware)
  sudo apt purge snapd

7 Clean Up Leftover Directories
  rm -rf ~/snap
  sudo rm -rf /var/snap /var/lib/snapd /var/cache/snapd

8 Reboot
  sudo reboot

9  System Updates: Ubuntu may reinstall Snapd during major upgrades. To prevent this, you can block Snapd from being reinstalled:
   sudo apt-mark hold snapd

10 Run sudo update-initramfs -u

Have Fun And Start Using Triaxis x86
